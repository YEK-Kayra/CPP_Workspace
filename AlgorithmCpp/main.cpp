#include <iostream>

using namespace std; 
S
int main() {

	//setlocale(LC_ALL, "Turkish");		//Türkçe karakter kullanmamızı sağlar. japonca bahsetsek o zaman japanese olur
	//cout << "üğ  ";

	/*if() 
	  if() 
	  şeklinde her kontrolde cpu gidip değerlendireceğin değişkende değeri kontrol edip yeniden alıyor
	  
	  else if()
	  else if()
	  
	  ile kontrol yaptırırsan static olarak değişkende ne varsa o kalmaya devam eder tekrardan değişkeni dışardan kontrol etmez
	  */

	/*
	int deger;
	cin >> deger;
	if ((13 > deger) && (5 < deger)) {
		cout << "Basarili";
	}
	*/

	/*

	int gun;
	cin >> gun;

	switch (gun) {

	case 1 : 
		cout << "Pzts";
		//break;			// Eğer buradaki break yok olursa 1 ve 2 yi yazar çümkü break görene kadar kamyon gibi devam eder aşağı
	case 2 : 
		cout << "slı";
		break;

	default : 
		cout << "Ynls deger";
	}

	*/
}